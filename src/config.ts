import 'dotenv/config';
const required=(name:string)=>{const value=process.env[name];if(!value)throw new Error(`Missing ${name}`);return value};
export const config={databaseUrl:required('DATABASE_URL'),accessSecret:required('JWT_ACCESS_SECRET'),refreshSecret:required('JWT_REFRESH_SECRET'),bootstrapSecret:required('SYNC_BOOTSTRAP_SECRET'),accessMinutes:Number(process.env.ACCESS_TOKEN_MINUTES||15),refreshDays:Number(process.env.REFRESH_TOKEN_DAYS||30),retentionDays:Number(process.env.TOMBSTONE_RETENTION_DAYS||30),port:Number(process.env.PORT||8787),origins:(process.env.CLIENT_ORIGINS||'').split(',').filter(Boolean)};
